name: imdreader-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis

  # C API
  - cython

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - MDAnalysisTests>=2.7.0
  - pwntools

  # gromacs
  - gromacs==2023.4

  # Pip-only installs
  #- pip:
  #  - codecov

